import 'dart:ui';

class AppColors{
  AppColors._();  //private constructor to prevent intatiation

static const Color gold = Color(0xFFFAC015);
static const Color green = Color(0xFF66680E);
static const Color bgColor = Color(0xFFF9FAFB);
}